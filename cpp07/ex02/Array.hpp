#pragma once

template <typename T>

class Array
{
    private:
        T *elements;
        unsigned int _size;
    public:
        Array();
        Array(unsigned int _size);
        size(T element);

};

template <typename T>

